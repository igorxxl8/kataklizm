function sum(a, b) {
    ret a + b
}

arr = array(1, "text", sum(10,15))
stdout(arr, "\n")
arr[0] = arr[0] + 1000 + arr[2]
stdout(arr)

arr1 = [1, 2, 3, 4, arr]
arr1[4][1] = "hello "
stdout(arr1)

stdout("\n", "\n")

array = init_array(2,2, 2, 2)
out array