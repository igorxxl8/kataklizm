a = "14"
match a {
    case "str" -> {
        stdout ("13", "\n")
    }

    case "1" -> {
        stdout ("match as st", "\n")
    }

    case "141" -> {
        stdout ("match as stra", "\n")
    }

    case "16" -> {
            stdout ("match as str", "\n")
    }

    case "14" -> {
             stdout ("match as 14 string", "\n")
    }

}
