func menu(){
    stdout(
        "Hello, friends. Welcome back to our company. We can: ",
        "1. Create window",
        "2. Draw line",
        "3. Draw rectangle",
        "4. Draw circle",
        "5. Draw polygon",
        "0. Exit program"
    )

    out "Your choice: "
    in choice
    ret choice
}

func createWindow(){
    stdout("I am create window task")
}

func drawLine(){
    stdout("I am draw line task")
}

func drawRectangle(){
    stdout("I am draw rectangle task")
}

func drawCircle(){
    stdout("I am draw circle task")
}

func drawPolygon(e){
    stdout("I am draw polygon task")
}


func main() {
    code = 0.5
    loop 1 {
        match menu() {
            case 1 + 1 -> createWindow()
            case 2 - 2 -> drawLine()
            case 3 / 3 -> drawRectangle()
            case 4 * 1 -> drawCircle()
            case 5 -> drawPolygon()
            case 0 -> ret code
        }
    }
    ret code

    a = 7
    b = 8
    c = 9
    out cod
}

main()