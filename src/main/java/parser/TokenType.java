package parser;

public enum TokenType {

    NUMBER,

    WORD,
    TEXT,

    PRINT,
    IF,
    ELSE,
    LOOP,
    FOR,
    BREAK,
    CONTINUE,

    PLUS,
    MINUS,
    STAR,
    SLASH,
    EQ,
    EQEQ,
    EXCL,
    EXCLEQ,
    LT,
    LTEQ,
    GT,
    GTEQ,

    BAR,
    BARBAR,
    AMP,
    AMPAMP,

    LBRACE,
    RBRACE,

    LPAREN,
    RPAREN,

    SEMI_COLON,
    COMMA,

    EOF
}
