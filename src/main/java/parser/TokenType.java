package parser;

public enum TokenType {

    NUMBER,

    WORD,
    TEXT,

    PRINT,
    IF,
    ELSE,

    PLUS,
    MINUS,
    STAR,
    SLASH,
    EQ,
    LT,
    GT,

    LPAREN,
    RPAREN,

    EOF
}
